env: bobail
agent: reinforce_mean_baseline
opponent: random

agent_params:
  lr: 0.0005
  gamma: 0.99
  hidden_layers: [256, 128]

training:
  num_episodes: 10000
  max_steps_per_episode: 500

eval:
  checkpoints: [1000, 2000, 40000, 5000, 10000]
  num_games: 50

seeds: [42, 123]
