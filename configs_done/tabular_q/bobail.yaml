env: bobail
agent: tabular_q
opponent: random

agent_params:
  lr: 0.1
  gamma: 0.99
  epsilon_start: 1.0
  epsilon_end: 0.05
  epsilon_decay_steps: 30000

training:
  num_episodes: 10000
  max_steps_per_episode: 200

eval:
  checkpoints: [1000, 5000, 10000]
  num_games: 50

seeds: [42]
